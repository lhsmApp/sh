<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TmplConfigMapper">

	<!--表名 -->
	<sql id="tableName">
		TB_TMPL_CONFIG_DETAIL
	</sql>

	<!-- 字段 -->
	<sql id="Field">
		DEPT_CODE,
		TABLE_CODE,
		COL_CODE,
		COL_NAME,
		DISP_ORDER,
		DICT_TRANS,
		COL_HIDE,
		COL_SUM,
		COL_AVE
	</sql>
	<sql id="FieldColumns">
		DEPT_CODE,
		TABLE_CODE,
		COL_CODE,
		COL_NAME,
		DISP_ORDER,
		DICT_TRANS,
		COL_HIDE,
		COL_SUM,
		COL_AVE
	</sql>

	<!-- 字段值 -->
	<sql id="FieldValue">
		#{DEPT_CODE},
		#{TABLE_CODE},
		#{COL_CODE},
		#{COL_NAME},
		#{DISP_ORDER},
		#{DICT_TRANS},
		#{COL_HIDE},
		#{COL_SUM},
		#{COL_AVE}
	</sql>

	<!-- 新增 -->
	<insert id="save" parameterType="pd">
		insert into
		<include refid="tableName"></include>
		(
		<include refid="Field"></include>
		) values (
		<include refid="FieldValue"></include>
		)
	</insert>

	<!-- 删除 -->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where
		TABLE_CODE = #{TABLE_CODE}
	</delete>
	
	<!-- 删除 -->
	<delete id="deleteTable" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where
		DEPT_CODE = #{DEPT_CODE}
		and
		TABLE_CODE = #{TABLE_CODE}
	</delete>

	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set
		<!-- DEPT_CODE = #{DEPT_CODE},
		TABLE_CODE = #{TABLE_CODE},
		COL_CODE = #{COL_CODE}, -->
		COL_NAME = #{COL_NAME},
		DISP_ORDER = #{DISP_ORDER},
		DICT_TRANS = #{DICT_TRANS},
		COL_HIDE = #{COL_HIDE},
		COL_SUM = #{COL_SUM},
		COL_AVE = #{COL_AVE}
		where
		DEPT_CODE = #{DEPT_CODE}
		and
		TABLE_CODE = #{TABLE_CODE}
		and
		COL_CODE = #{COL_CODE}
	</update>

	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where
		TABLE_CODE = #{TABLE_CODE}
	</select>

	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		<where>
			<if test="pd.DEPT_BIANMA!= null and pd.DEPT_BIANMA != ''"><!-- 检索 -->
				and (
				DEPT_CODE = #{pd.DEPT_BIANMA}
				)
			</if>
			<if test="pd.TABLE_CODE!= null and pd.TABLE_CODE != ''"><!-- 检索 -->
				and (
				TABLE_CODE = #{pd.TABLE_CODE}
				)
			</if>
		</where>
	</select>

	<!-- 列表(全部) -->
	<select id="listAll" parameterType="page" resultType="pd">
		SELECT
		a.DEPT_CODE,
		a.TABLE_CODE,
		a.COL_CODE,
		a.COL_NAME,
		a.DISP_ORDER,
		a.DICT_TRANS,
		a.COL_HIDE,
		a.COL_SUM,
		a.COL_AVE,
		b.NAME DNAME,
		c.TABLE_NAME,
		d.DICT_NAME
		FROM
		tb_tmpl_config_detail a LEFT JOIN oa_department b
		on a.DEPT_CODE = b.DEPARTMENT_CODE
		LEFT JOIN tb_tmpl_config_base c
		on a.TABLE_CODE = c.TABLE_CODE
		LEFT JOIN tb_tmpl_config_dict d
		on a.DICT_TRANS = d.DICT_CODE
		where 1=1
			<if test="pd.DEPARTMENT_CODE!= null and pd.DEPARTMENT_CODE != ''">
				and (
				a.DEPT_CODE = #{pd.DEPARTMENT_CODE}
				)
			</if>
			<if test="pd.TABLE_CODE!= null and pd.TABLE_CODE != ''">
				and (
				a.TABLE_CODE = #{pd.TABLE_CODE}
				)
			</if>
		
	</select>

	<!-- 批量删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where
		TABLE_CODE in
		<foreach item="item" index="index" collection="array" open="("
			separator="," close=")">
			#{item}
		</foreach>
	</delete>

	<!-- 基本配置表 -->
	<select id="listBase" parameterType="page" resultType="pd">
		select *
		from TB_TMPL_CONFIG_BASE
	</select>

	<!-- 根据当前单位编码及表名获取字段配置信息 -->
	<select id="listNeed" parameterType="com.fh.entity.TmplConfigDetail" 
	        resultType="com.fh.entity.TmplConfigDetail">
		select
		    <include refid="FieldColumns"></include>
		from 
		    <include refid="tableName"></include>
		where DEPT_CODE = #{DEPT_CODE}
	    and UPPER(TABLE_CODE) = UPPER(#{TABLE_CODE})
	    order by DISP_ORDER
	</select>
	
	<!-- 根据TABLE_NO获取TABLE_CODE -->
	<select id="findTableCodeByTableNo" parameterType="String" resultType="pd">
		select TABLE_CODE
		from TB_TMPL_CONFIG_BASE
		where
		TB_TMPL_CONFIG_BASE.TABLE_NO = #{TABLE_NO}
	</select>
	
	<!-- 临时生成的数据表明细 -->
	<select id="temporaryList" parameterType="page" resultType="pd">
		SELECT column_name AS COL_CODE, column_comment AS COL_NAME,
		#{pd.DEPARTMENT_CODE} AS DEPT_CODE,#{pd.TABLE_CODE} AS TABLE_CODE,#{pd.DNAME} AS DNAME,#{pd.TABLE_NAME} AS TABLE_NAME,
		'0' AS DISP_ORDER,'0' AS DICT_TRANS,'1' AS COL_HIDE,'0' AS COL_SUM,'0' AS COL_AVE
		FROM 
 		information_schema.columns 
 		WHERE 
 		table_schema ='dbgdhs' 
 		AND 
 		table_name = #{pd.TABLE_CODE}
	</select>
	<!-- 字典翻译集合 -->
	<select id="dictList" parameterType="pd" resultType="pd">
		select DICT_CODE, DICT_NAME
		from tb_tmpl_config_dict
	</select>
	
	<!-- 批量保存 -->
	<!--  <update id="updateAll" parameterType="pd" >
		<foreach item="item" index="index" collection="list" open=" " separator="; " close=" ">
		    <if test="item!=null">
		        update
		        <include refid="tableName"></include>
		        set 
		    	    COL_NAME = #{item.COL_NAME},
					DISP_ORDER = #{item.DISP_ORDER},
					DICT_TRANS = #{item.DICT_NAME},
					COL_HIDE = #{item.COL_HIDE},
					COL_SUM = #{item.COL_SUM},
					COL_AVE = #{item.COL_AVE}
				where
					DEPT_CODE = #{item.DEPT_CODE}
				and
					TABLE_CODE = #{item.TABLE_CODE}
				and
					COL_CODE = #{item.COL_CODE}
		    </if>
		</foreach>
	</update>  -->
	
	<!-- 新增 -->
	 <insert id="updateAll" parameterType="pd">
		<!-- 插入保存 -->
				insert into
				<include refid="tableName"></include>
				(
				<include refid="Field"></include>
				) values 
				<foreach item="item" index="index" collection="list" open=" " separator=", " close=" ">
		    <if test="item!=null">
				<trim prefix="(" suffix=")" suffixOverrides=",">
				#{item.DEPT_CODE},#{item.TABLE_CODE},#{item.COL_CODE},#{item.COL_NAME},#{item.DISP_ORDER},#{item.DICT_TRANS},#{item.COL_HIDE},#{item.COL_SUM},#{item.COL_AVE}
				<!-- <include refid="FieldValue"></include> -->
				</trim>
		</if>
		</foreach>
	</insert>
	
	<!-- 复制 -->
	 <insert id="copyAll" parameterType="String">
		<!-- 插入保存 -->
				insert into
				<include refid="tableName"></include>
				(
				<include refid="Field"></include>
				)  
				<foreach item="item" index="index" collection="list" open=" " separator="union all " close=" ">
		    <if test="item!=null">
				SELECT #{item} AS 'DEPT_CODE', TABLE_CODE,COL_CODE,COL_NAME,DISP_ORDER,DICT_TRANS,COL_HIDE,COL_SUM,COL_AVE
 				FROM tb_tmpl_config_detail WHERE DEPT_CODE='001' AND TABLE_CODE='TB_HOUSE_FUND_AUDIT'
		</if>
		</foreach>
	</insert>

	<!-- 获取数据库中表结构-->
	<select id="getTableColumns" parameterType="string" resultType="com.fh.entity.TableColumns">
	    SELECT UPPER(column_name) column_name, 
	           column_comment, 
	           UPPER(data_type) data_type, 
	           UPPER(column_type) column_type,
	           UPPER(column_key) column_key, 
	           column_default 
	    FROM information_schema.columns 
	    WHERE table_schema ='dbgdhs' 
 		AND  
	    table_name = #{value}
	</select>
</mapper>
