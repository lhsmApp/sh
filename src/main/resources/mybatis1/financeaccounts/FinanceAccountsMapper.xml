<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="FinanceAccountsMapper">
	
	
	<!-- 列表 -->
	<select id="datalistJqPage" parameterType="jqPage" resultType="pd">
		select ${pd.SelectFeild}
		from ${pd.TableName}
		where BUSI_DATE = '${pd.SystemDateTime}'
		<if test="pd.filterWhereResult!= null and pd.filterWhereResult != ''">
			${pd.filterWhereResult}
		</if>
		<if test="pd.SallaryType!= null and pd.SallaryType != ''">
		    and ${pd.SallaryType}
		</if>
	    group by ${GroupbyFeild}
	</select>
	
	<select id="datalistDetail" parameterType="pd" resultType="pd">
		select * 
		from ${TableName}
		where BUSI_DATE = #{BUSI_DATE}
		and DEPT_CODE = '#{DEPT_CODE}
		<if test="filterWhereResult!= null and filterWhereResult != ''">
			${filterWhereResult}
		</if>
		<if test="SallaryType!= null and SallaryType != ''">
		    and ${SallaryType}
		</if>
	</select>
</mapper>